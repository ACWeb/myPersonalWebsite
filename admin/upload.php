<?php

// Used for uploading admin content, such as blog posts and images.

if ($upload_type = "post") {
	
	// Upload the post
	
} else if ($upload_type = "image") {

	// Upload the image
	
} else if ($upload_type = "static-page") {

	// Upload the static page
	
}  else {
	die("Unknown Upload Format");
}